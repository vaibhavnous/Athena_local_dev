name: Deploy Frontend to Astradata

on:
  push:
    branches:
      - main
    paths:
      - frontend/**
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: astradata-frontend-production
  cancel-in-progress: true

env:
  REACT_APP_API_BASE_URL: https://astra-data-eecthacqb5eherhk.southindia-01.azurewebsites.net
  REACT_APP_ENABLE_DEMO_FALLBACKS: "false"

jobs:
  build-and-deploy:
    name: Build and deploy frontend
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: Test frontend
        working-directory: frontend
        run: npm test -- --watchAll=false --runInBand

      - name: Build frontend
        working-directory: frontend
        run: npm run build

      - name: Deploy to Astradata Static Web App
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_ASTRADATA }}
          action: upload
          app_location: frontend/build
          api_location: ""
          output_location: ""
          skip_app_build: true
